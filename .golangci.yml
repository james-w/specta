version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gocyclo
    - unconvert

  disable:
    - depguard
    - dupl  # Disabled because test files naturally have similar structure

run:
  timeout: 5m
